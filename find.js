const arr = [5, 12, 8, 130, 44]
const resultado = arr.find(x => x > 10)
console.log(resultado)
// devuelve el valor del primer elemento del array que cumple la condicion.
// return: 12
//si no hay coincidencias, retorna undefined